import styled from 'styled-components'

export const Form = styled.form`
  max-widthn: 560px;
  width: 100%;
  font-weight: bold;
  font-size: 14px;

  input {
    margin-bottom: 8px;
  }
`

export const Opcoes = styled.div`
  margin-bottom: 16px;

  p {
    margin-bottom: 6px;
  }

  label {
    margin-right: 6px;
  }
`
